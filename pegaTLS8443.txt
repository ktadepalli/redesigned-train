global:
  actions:
    execute: install-deploy

web:
  tls:
    enabled: true
    keystoreType: "JKS"             # or "PKCS12" if your file is .p12
    keystorePath: "/opt/pega/certs/pega-keystore.jks"
    keystorePassword: "changeit"
    keyAlias: "tomcat"
    port: 8443                      # HTTPS port inside pod
  service:
    httpsPort: 8443
    httpPort: 80                    # optional, keep both 80 and 8443
    type: ClusterIP

  volumeMounts:
    - name: tls-keystore
      mountPath: /opt/pega/certs
  volumes:
    - name: tls-keystore
      secret:
        secretName: pega-tls-keystore-secret
